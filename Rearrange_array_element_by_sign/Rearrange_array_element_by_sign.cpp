// writing the code for the an array where we have to arrange the number in such away that 
// after every positive number there should be negative number
#include<bits/stdc++.h>
using namespace std;

vector<int>rearrange(vector<int>&nums){
    int n = nums.size();
    // for storing the changed array in vector
    vector<int>result(n);
       int po = 0;   //here w
       int ne = 1
}